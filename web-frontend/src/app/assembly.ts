export interface Assembly {
    id: number;
    name: string;
    assembly: string;
    /*
      lines of generated assembly

      errors & warnings if present with line numbers

      console output if present

    */
  }